{
  "name": "zxlab-risk-market-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:beta": "wrangler deploy --config wrangler.beta.jsonc",
    "types": "wrangler types",
    "types:beta": "wrangler types worker-configuration.beta.d.ts --config wrangler.beta.jsonc --env-interface ResearchEnv --include-runtime false --strict-vars false",
    "typecheck": "wrangler types && npm run types:beta && tsc --noEmit",
    "test": "node --test --experimental-transform-types src/*.test.ts src/research/*.test.ts"
  },
  "dependencies": {
    "@zxlab/research-fact-schema": "file:../../packages/research-fact-schema"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "wrangler": "^4.110.0"
  }
}
