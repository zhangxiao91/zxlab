RISK_DATABASE_URL=postgresql+psycopg://zxlab:local-risk-only@127.0.0.1:54329/zxlab_risk
RISK_PROVIDER_MODE=mock
RISK_REVIEW_MODE=mock
RISK_OPENAI_MODEL=gpt-5-mini
RISK_OPENAI_ENV_FILE=/Users/zhangyang/Developer/.env
# OPENAI_API_KEY is read server-side only. Never expose it through PUBLIC_ variables.
# The personal parent env may alternatively expose apikey and baseurl; they are mapped server-side.
