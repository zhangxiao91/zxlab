services:
  wechat-ai-bot:
    build: .
    container_name: wechat-ai-bot
    restart: unless-stopped
    init: true
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./credentials:/app/credentials
      - ./persona.md:/app/persona.md:ro
