任务：只修改 hello.html，为页面增加一个可由键盘操作的按钮。
当前行为：页面显示“Hello World”。
目标行为：点击按钮后在“Hello World”和“你好，大学”之间切换。
限制：不新增依赖，不访问网络，不修改其他文件，不读取任何凭证或个人资料。
验收：页面可直接打开；按钮可点击和键盘聚焦；刷新后恢复初始文字；Console 无报错。
工作方式：先说明计划和将修改的文件；给出最小改动；完成后列出逐项验证步骤和恢复方法。
