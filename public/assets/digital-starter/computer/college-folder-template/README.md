# 大学资料文件夹模板

这套目录用于区分收件、课程、手续、项目、作品、安装包和归档。

使用方法：

1. 把整个 `大学资料` 文件夹复制到自己的文档或同步目录。
2. 将示例学期和课程改成真实名称。
3. 每周清理一次 `00_收件箱`。
4. 重要证件不要放进公开共享目录，至少保留一份独立备份。

如果目录尚未创建，可先阅读 `create-folders.ps1`，确认内容后在 Windows PowerShell 中运行。
